# Urgen-See API Server

## Environment Variables
This server uses [dotenv](https://www.npmjs.com/package/dotenv) for environment variables. Create a file named `.env` with the following information:

- **DATABASE_URL**